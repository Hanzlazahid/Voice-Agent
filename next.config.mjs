/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/Voice-Agent',
    assetPrefix: '/Voice-Agent',
    trailingSlash: true,
};

export default nextConfig;
